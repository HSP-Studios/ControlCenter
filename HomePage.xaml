<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ControlCenter.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ControlCenter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer
            ZoomMode="Disabled"
            IsTabStop="True" 
            IsVerticalScrollChainingEnabled="True"
            HorizontalAlignment="Left" 
            VerticalAlignment="Top"
            HorizontalScrollMode="Disabled" 
            HorizontalScrollBarVisibility="Hidden"
            VerticalScrollMode="Enabled" 
            VerticalScrollBarVisibility="Hidden">

            <StackPanel x:Name="MainContentPanel" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="70,70,0,0">
                <TextBlock FontFamily="{StaticResource Bold}" FontSize="80" Text="{x:Bind GreetingText}"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
